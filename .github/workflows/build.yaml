name: ROS build workflow

on:
  workflow_call:
    inputs:
      os:
        required: true
        type: string
      ros_distro:
        required: true
        type: string

jobs:
  ros_build:
    runs-on: ${{ inputs.os }}
    env:
      WS: /home/runner/work/ros2_ws
    steps:
    - uses: actions/checkout@v4
      with:
        path: ddynamic_reconfigure2

    - name: Setup ROS
      uses: ros-tooling/setup-ros@v0.7
      with:
        required-ros-distributions: ${{ inputs.ros_distro }}

    - name: apt update and upgrade
      run: |
        sudo apt update
        sudo apt upgrade -y

    - name: Install dependencies
      run: |
        sudo apt install nlohmann-json3-dev python3-rosdoc2

    - name: Checkout dependencies
      run: |
        vcs import . --input=ddynamic_reconfigure2/dependencies.repos

    - name: Init workspace
      run: |
        mkdir ${WS}
        ln -s ${GITHUB_WORKSPACE} ${WS}/src

    - name: Pre build
      run: |
        source /opt/ros/${{ inputs.ros_distro }}/setup.bash
        cd ${WS}/src
        rosdep update
        rosdep install -iy --from-paths ./

    - name: Build
      run: |
        source /opt/ros/${{ inputs.ros_distro }}/setup.bash
        cd ${WS}
        colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=Release

    - name: Install doxyfilter_python
      run: |
        sudo install ./doxyfilter_python/doxyfilter_python.py /usr/local/bin

    - name: Build documents of ddynamic_reconfigure2
      run: |
        cd ${WS}/src/ddynamic_reconfigure2
        doxygen

    - name: Build documents of ddynamic_reconfigure2_msgs
      run: |
        cd ${WS}/src/ddynamic_reconfigure2/docs
        rosdoc2 build --package-path ../ddynamic_reconfigure2_msgs
        mv ./docs_output/ddynamic_reconfigure2_msgs ./html

    - name: Upload artifact of ddynamic_reconfigure2
      uses: actions/upload-pages-artifact@v4
      with:
        path: ./ddynamic_reconfigure2/docs/html

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v5
